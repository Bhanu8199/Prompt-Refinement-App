## Packages
framer-motion | Smooth animations for the refinement process and page transitions
date-fns | Formatting timestamps for history
clsx | Conditional class names
tailwind-merge | Merging Tailwind classes safely

## Notes
The app uses a dark-themed, professional aesthetic suitable for AI tools.
Refined prompt data is stored as JSONB, so the frontend expects a structured object matching the `refinedOutputSchema`.
